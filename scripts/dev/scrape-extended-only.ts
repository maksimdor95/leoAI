/**
 * Moved to services/job-matching/scripts/scrape-extended-only.ts
 * Run: cd services/job-matching && npx ts-node --transpile-only scripts/scrape-extended-only.ts
 */
console.error(
  'Use: cd services/job-matching && npx ts-node --transpile-only scripts/scrape-extended-only.ts'
);
process.exit(1);
